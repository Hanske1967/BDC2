apply plugin: "war"

bootWar {
    mainClassName = "be.mloz.bdc.Bdc2App"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
